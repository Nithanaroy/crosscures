[project]
name = "crosscures"
version = "0.1.0"
description = "EHR data summarizer with Epic FHIR integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.4",
    "pandas>=3.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
