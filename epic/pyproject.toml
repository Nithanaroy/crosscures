[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "epic"
version = "0.1.0"
description = "Epic FHIR API client and clinical data summarizer"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
